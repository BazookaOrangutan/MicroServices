spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:postgresql://localhost:5432/ms
    username: postgres
    password: 142536
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  port: 0
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka